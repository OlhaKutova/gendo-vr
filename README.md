# ✨ Gendo VR – Marketing Landing Page

This project is a single-page marketing website for the **Gendo VR** feature.

---

## 🚀 Getting Started

```bash
npm install
npm run dev
```

Open http://localhost:3000 in the browser.

---

## 🛠 Tech Stack

- **Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Styling:** CSS Modules (BEM naming convention) & CSS Variables
- **Animation:** Framer Motion
- **Fonts:** Geist Sans & Mono (via `next/font`)

---

## 📂 Project Structure

```txt
src/
├── app/                 # Next.js App Router (routing only)
├── components/          # Reusable UI primitives (Buttons, Typography)
├── containers/          # Page sections (Hero, Workflow, Features, FAQ, Social Proof, Footer)
├── content/             # Static data / text strings (separated from logic)
└── styles/              # Global variables, reset
```
